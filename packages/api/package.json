{
  "name": "@moonlight/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "hono": "^3.0.0",
    "@hono/zod-validator": "^0.1.0",
    "zod": "^3.0.0"
  }
} 